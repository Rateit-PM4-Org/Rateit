apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: base
  labels:
    app: rateit
    env: base

resources:
- api.yaml
- db.yaml
- configMap.yaml
- secrets.yaml

commonLabels:
  app: rateit
  env: base
